apiVersion: v1
kind: Service
metadata:
  name: scalable-pod-identifier-service
  labels:
    app: scalable-pod-identifier
spec:
  type: NodePort
  selector:
    app: scalable-pod-identifier
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
